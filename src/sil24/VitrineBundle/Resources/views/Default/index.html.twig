{% extends "sil24VitrineBundle::layout.html.twig" %}

{% block body %}

{#
<div>
#}
{#    {% if(app.session.get('client') == 'anon') %}
        <h3>Bonjour !</h3>
    {% else %}
        <h3>Bonjour {{ app.session.get('client').getName() }} !</h3>
    {% endif %}#}

    <div class="carousel">
        {% for article in articles %}
            <a class="carousel-item" href="#"><img src="{{ asset('bundles/sil24vitrine/img/'~article.picture.getNom())}}"></a>
        {% endfor %}
    </div>

    <script>
        $(document).ready(function(){
            $('.carousel').carousel();
        });
    </script>
{#
</div>
#}

{% endblock %}
